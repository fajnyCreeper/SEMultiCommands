CREATE TABLE `patternList`(
  `pattern` varchar(10) NOT NULL,
  `message1` varchar(500) DEFAULT NULL,
  `message2` varchar(500) DEFAULT NULL,
  `message3` varchar(500) DEFAULT NULL,
  `message4` varchar(500) DEFAULT NULL,
  `message5` varchar(500) DEFAULT NULL,
  PRIMARY KEY (`pattern`)
)
